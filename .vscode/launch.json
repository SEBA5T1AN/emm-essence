{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "EMMEssence",
            "type": "debugpy",
            "request": "launch",
            "module": "emmessence.cli",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src",
                "PATH": "${workspaceFolder}\\.venv\\Scripts;${env:PATH}"
            }
        }
    ]
}